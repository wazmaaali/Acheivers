version: 2.1

jobs:
  build-and-test-javascript:
    docker:
      - image: cimg/node:14.20.0
    steps:
      - checkout
      - run:
          name: install dependencies
          command: cd ./backend && npm install

      - run:
          name: run test
          command: cd ./backend/test && npm run test
workflows:
  sample:
    jobs:
      - build-and-test-javascript
